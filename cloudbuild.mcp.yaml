steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile.mcp'
      - '-t'
      - 'gcr.io/$PROJECT_ID/recruitment-mcp-server'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/recruitment-mcp-server'
options:
  logging: CLOUD_LOGGING_ONLY